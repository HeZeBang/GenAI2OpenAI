services:
  genai2openai:
    image: ghcr.io/hezebang/genai2openai:main
    ports:
      - "${PORT:-5000}:5000"
    environment:
      - API_KEY=${API_KEY:-}
    command: ["--token", "${TOKEN}", "--port", "5000"]
    restart: unless-stopped
